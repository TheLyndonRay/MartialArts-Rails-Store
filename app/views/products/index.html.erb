<h1>Products List</h1>
<main>
    <% @products.each do |product| %>
        <section>
          <%= "#{product.name}" %>
          <%= "#{product.price} each" %>
          <%= image_tag product.image %>
        </section>
    <% end %>
</main>

<p>Find me in app/views/products/index.html.erb</p>
